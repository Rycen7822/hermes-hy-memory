name: hy_memory
version: 0.1.0
kind: standalone
description: "HY Memory local/remote cognitive long-term memory provider for Hermes Agent. Uses the hy-memory SDK with explicit tools, auto recall, auto capture, and a bundled memory-curation skill."
pip_dependencies:
  - hy-memory
